---
  apiVersion: v1
  kind: ConfigMap
  metadata:
    name: kube-monkey-config-map
    namespace: kube-system
  data:
    config.toml: |
      [kubemonkey]
      run_hour = 20
      start_hour = 20
      end_hour = 21
      blacklisted_namespaces = ["kube-system", "prod"]